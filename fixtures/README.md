# Fixtures

Every `fixtures/*.tex` file is automatically enrolled as a canonical fixture property baseline. Helper
fixtures are excluded only by the reserved prefixes `lint-` and `measure-`, or the filename pattern
`deck-*-preamble.tex`.
