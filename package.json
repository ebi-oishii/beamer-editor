{
  "name": "beamer-editor",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "biome check .",
    "format": "biome check --write .",
    "build:skills": "pnpm --filter @beamer-editor/cli build:skills",
    "check:skills": "pnpm --filter @beamer-editor/cli build:skills --check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}
